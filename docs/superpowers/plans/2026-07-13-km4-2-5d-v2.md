# KM4/F151 Repair-Grade 2.5D V2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the KM4/F151 debug geometry view into a source-honest, repair-grade orthographic 2.5D board model.

**Architecture:** A pure model-profile module converts compiled component records into conservative render descriptors. `BoardRenderer` consumes those descriptors to build a layered Three.js scene with a clipped engineering surface, flat uncertainty overlays, procedural package meshes, and stable camera controls. Existing registration and evidence modules remain the source of selection state.

**Tech Stack:** JavaScript ES modules, Three.js, HTML/CSS, Node test runner, Python validation, Playwright/browser QA.

---

### Task 1: Render Profile Contract

**Files:**
- Create: `assets/cross-source-registration/model-profiles.js`
- Create: `tests/model-profiles.test.mjs`

- [ ] Write failing tests for confidence-layer rules, package-family mapping, dimension caps, and conservative heights.
- [ ] Run `node --test tests/model-profiles.test.mjs` and confirm failures are caused by the missing module.
- [ ] Implement pure `buildRenderProfile`, `buildRenderDescriptor`, and `buildCameraFrame` functions.
- [ ] Run the targeted test and the complete Node test suite.

### Task 2: Layered Orthographic Renderer

**Files:**
- Modify: `assets/cross-source-registration/board-renderer.js`
- Modify: `assets/cross-source-registration/app.js`

- [ ] Pass source image and confidence-bearing compiled component records into the renderer.
- [ ] Replace the perspective camera with a fitted orthographic camera and deterministic top-down reset.
- [ ] Build the board substrate, clipped engineering texture, flat medium-confidence outlines, and elevated high-confidence package bodies.
- [ ] Keep reviewed entities as the only primary raycast targets and use emissive/outline selection feedback.
- [ ] Run Node and Python registration tests.

### Task 3: Technician View Controls

**Files:**
- Modify: `assets/cross-source-registration/index.html`
- Modify: `assets/cross-source-registration/styles.css`
- Modify: `assets/cross-source-registration/app.js`

- [ ] Add icon controls for top-down reset and inspection angle, plus a compact confidence legend.
- [ ] Preserve stable stage dimensions at desktop and mobile widths.
- [ ] Update visible model guidance to match constrained interaction behavior.
- [ ] Run static frontend validation.

### Task 4: Visual And Interaction QA

**Files:**
- Modify: `docs/km4-cross-source-registration-2026-07-13.md`
- Modify: project status records only after implementation evidence exists.

- [ ] Start the existing local server and open the real cross-source registration route.
- [ ] Verify desktop and mobile framing, nonblank canvas pixels, point-map surface, component scale, and no overlap.
- [ ] Exercise model tab, selection, zoom, rotation, inspection angle, top-down reset, and evidence synchronization.
- [ ] Check default, hover, focus, selected states and browser runtime errors.
- [ ] Run all relevant Node/Python tests and record P0-P3 evidence and residual source limitations.

